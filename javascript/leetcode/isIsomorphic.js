/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isIsomorphic = function(s, t) {
    // determine if the same length
    // map one char to another char from s to t, and if it exists determine if it's the same char. return false if not
    // otherwise, add to the hashmap and if at the end, return true.

};
